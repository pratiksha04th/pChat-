class ReactionIconModel{
  double left;
  double size;
  double speed;
  double drift;

  ReactionIconModel({
    required this.left,
    required this.size,
    required this.speed,
    required this.drift,
  });
}